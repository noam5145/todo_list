import React, { useContext, useEffect, useState } from 'react'
import { MyContext } from './App';


export default function Socket({item, i}) {
    const {currentUser, missions, setMissions} = useContext(MyContext);
    

  return (
    <div>
        
    </div>
  )
}
